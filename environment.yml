name: pokemon
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - torchvision
  - pytorch
  - torchaudio
  - pytorch-cuda=12.1
  - pip
  - pip:
      - poke-env
      - pyyaml
      - tqdm